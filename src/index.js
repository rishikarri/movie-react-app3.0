import React from 'react';
import ReactDOM from 'react-dom';

// we need to bring in the router and the route from the react-router node module

import App from './App';
import './index.css';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
